const $=s=>document.querySelector(s);const allSchools=[...new Set(GROUPS.flatMap(g=>g[3]))].sort();$('#schools').innerHTML=allSchools.map(s=>`<option value="${s}">`).join('');
function lookup(){const q=$('#school').value.trim();const found=GROUPS.filter(g=>g[3].includes(q));if(!q||!found.length){$('#basicResult').className='result empty';$('#basicResult').textContent='일치하는 학교를 찾지 못했습니다.';return}$('#basicResult').className='result';$('#basicResult').innerHTML=found.map(g=>`<div><span class="badge">${g[1]}</span><strong>${g[0]}</strong><div class="schools">${g[2].map(x=>`<span class="school-tag">${x}</span>`).join('')}</div>${g[4]?`<p>※ ${g[4]}</p>`:''}</div>`).join('<hr>')}
$('#lookup').onclick=lookup;$('#school').addEventListener('keydown',e=>{if(e.key==='Enter')lookup()});
$('#conditions').innerHTML=SPECIAL.map((x,i)=>`<button class="chip" data-i="${i}">${x[0]}</button>`).join('');document.querySelectorAll('.chip').forEach(b=>b.onclick=()=>{b.classList.toggle('active');const sel=[...document.querySelectorAll('.chip.active')].map(x=>SPECIAL[+x.dataset.i]);$('#conditionResult').className=sel.length?'result':'result empty';$('#conditionResult').innerHTML=sel.length?sel.map(x=>`<div class="plan-item"><strong>${x[0]}</strong><br>${x[1]}</div>`).join(''):'선택한 조건이 없습니다.'});
function renderPlan(type){let data=type==='general'?GENERAL:type==='mixed'?MIXED:SPECIAL;$('#plan').innerHTML=data.map(x=>`<div class="plan-item"><strong>${x[0]}</strong><br>${x.slice(1).join(' · ')}</div>`).join('')}renderPlan('general');document.querySelectorAll('.tab').forEach(b=>b.onclick=()=>{document.querySelectorAll('.tab').forEach(x=>x.classList.remove('active'));b.classList.add('active');renderPlan(b.dataset.tab)});
